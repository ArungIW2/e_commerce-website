@extends('store.layout', ['title' => 'Products'])
@section('content')<section class="mx-auto max-w-7xl px-6 py-12"><div class="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p class="text-sm font-bold uppercase tracking-widest text-indigo-600">Catalog</p><h1 class="mt-2 text-4xl font-black">Products</h1></div><form><input name="q" value="{{ request('q') }}" placeholder="Search products..." class="rounded-xl border px-4 py-3 outline-none focus:ring-2 focus:ring-indigo-500"></form></div><div class="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">@forelse($products as $product)<article class="rounded-2xl border bg-white p-5 shadow-sm"><a href="{{ route('products.show', $product) }}" class="block"><div class="aspect-square rounded-xl bg-slate-100 overflow-hidden">@if($product->image)<img src="{{ $product->image }}" alt="{{ $product->name }}" class="h-full w-full rounded-xl object-cover">@endif</div><h2 class="mt-4 font-bold">{{ $product->name }}</h2><p class="mt-2 font-black">Rp {{ number_format($product->price, 0, ',', '.') }}</p></a>@auth<form method="POST" action="{{ route('wishlist.toggle', $product) }}" class="mt-4">@csrf<button class="w-full rounded-xl border px-3 py-2 text-sm font-bold hover:bg-slate-50">♡ Wishlist</button></form>@endauth</article>@empty<p>No products found.</p>@endforelse</div><div class="mt-10">{{ $products->links() }}</div></section>@endsection
