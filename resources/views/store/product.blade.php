@extends('store.layout', ['title' => $product->name])
@section('content')<section class="mx-auto grid max-w-7xl gap-12 px-6 py-16 md:grid-cols-2"><div class="aspect-square rounded-3xl bg-slate-100">@if($product->image)<img src="{{ $product->image }}" class="h-full w-full rounded-3xl object-cover">@endif</div><div class="py-6"><p class="text-sm font-bold uppercase tracking-widest text-indigo-600">{{ $product->category?->name }}</p><h1 class="mt-3 text-4xl font-black">{{ $product->name }}</h1><p class="mt-5 text-3xl font-black">Rp {{ number_format($product->price, 0, ',', '.') }}</p><p class="mt-6 leading-7 text-slate-600">{{ $product->description ?: 'Product description will appear here.' }}</p><p class="mt-6 text-sm text-slate-500">Stock: {{ $product->stock }}</p><button class="mt-8 rounded-xl bg-slate-950 px-7 py-3 font-bold text-white">Add to cart</button></div></section>@endsection
