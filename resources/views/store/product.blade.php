@extends('store.layout', ['title' => $product->name])
@section('content')<section class="mx-auto grid max-w-7xl gap-12 px-6 py-16 md:grid-cols-2"><div class="aspect-square rounded-3xl bg-slate-100">@if($product->image)<img src="{{ $product->image }}" class="h-full w-full rounded-3xl object-cover">@endif</div><div class="py-6"><p class="text-sm font-bold uppercase tracking-widest text-indigo-600">{{ $product->category?->name }}</p><h1 class="mt-3 text-4xl font-black">{{ $product->name }}</h1><p class="mt-5 text-3xl font-black">Rp {{ number_format($product->price, 0, ',', '.') }}</p><p class="mt-6 leading-7 text-slate-600">{{ $product->description ?: 'Product description will appear here.' }}</p><p class="mt-6 text-sm text-slate-500">Stock: {{ $product->stock }}</p>@if($product->stock > 0)<form method="POST" action="{{ route('cart.add', $product) }}" class="mt-8 flex items-center gap-3">@csrf<input type="number" name="quantity" value="1" min="1" max="{{ $product->stock }}" class="w-24 rounded-xl border px-4 py-3"><button class="rounded-xl bg-slate-950 px-7 py-3 font-bold text-white">Add to cart</button></form>@else<div class="mt-8 rounded-xl bg-red-50 px-5 py-3 font-bold text-red-600">Out of stock</div>@endif</div></section>@endsection
